{
  "name": "gavinsdavies.com",
  "private": true,
  "scripts": {
    "sync:cv": "sh scripts/sync-cv.sh",
    "build": "npm run sync:cv && hugo --gc --minify",
    "dev": "npm run sync:cv && hugo server --disableFastRender"
  }
}
